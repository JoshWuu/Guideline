<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:opencv="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <org.opencv.android.JavaCameraView
        android:id="@+id/java_camera_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible"
        opencv:show_fps="true"
        opencv:camera_id="back" />

    <!-- Button to toggle calibration panel -->
    <Button
        android:id="@+id/btn_calibrate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:text="Calibration" />

    <!-- Calibration Panel (Initially Hidden) -->
    <LinearLayout
        android:id="@+id/calibration_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#80000000"
        android:orientation="vertical"
        android:padding="8dp"
        android:visibility="gone">

        <!-- Min Distance parameter -->
        <TextView
            android:id="@+id/tv_min_dist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Min Distance: 10"
            android:textColor="#FFFFFF" />

        <SeekBar
            android:id="@+id/sb_min_dist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="50"
            android:progress="10" />

        <!-- Canny Edge Detector parameter -->
        <TextView
            android:id="@+id/tv_param1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Param1 (Canny): 50"
            android:textColor="#FFFFFF" />

        <SeekBar
            android:id="@+id/sb_param1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="300"
            android:progress="50" />

        <!-- Accumulator Threshold parameter -->
        <TextView
            android:id="@+id/tv_param2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Param2 (Threshold): 30"
            android:textColor="#FFFFFF" />

        <SeekBar
            android:id="@+id/sb_param2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="30" />

        <!-- Min Radius parameter -->
        <TextView
            android:id="@+id/tv_min_radius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Min Radius: 3"
            android:textColor="#FFFFFF" />

        <SeekBar
            android:id="@+id/sb_min_radius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="50"
            android:progress="3" />

        <!-- Max Radius parameter -->
        <TextView
            android:id="@+id/tv_max_radius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Max Radius: 10"
            android:textColor="#FFFFFF" />

        <SeekBar
            android:id="@+id/sb_max_radius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="10" />

    </LinearLayout>
</FrameLayout>